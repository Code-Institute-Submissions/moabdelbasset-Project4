from django.apps import AppConfig


class NutritionConfig(AppConfig):
    """AppConfig subclass for the 'nutrition' application"""
    default_auto_field = "django.db.models.BigAutoField"
    name = "nutrition"
