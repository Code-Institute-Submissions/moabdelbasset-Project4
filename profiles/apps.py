from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """AppConfig subclass for the 'profiles' application"""
    default_auto_field = "django.db.models.BigAutoField"
    name = "profiles"
